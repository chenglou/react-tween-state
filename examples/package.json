{
  "name": "react-tween-state-example",
  "version": "0.0.0",
  "description": "React animation.",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node_modules/.bin/browserify --extension=.jsx index.jsx > out.js"
  },
  "author": "Cheng Lou",
  "license": "BSD",
  "devDependencies": {
    "browserify": "^6.3.2",
    "reactify": "^0.16.0"
  },
  "dependencies": {
    "react": "^0.12.0"
  },
  "browserify": {
    "transform": [
      ["reactify"]
    ]
  }
}
